const whatCohortIsBest = irrelevantNumberArgument => {
  // do a bunch of stuff with argument
  const favEpisodeNumberOfTheOffice = 112 // <- Scott's Tots
  const newVariable = irrelevantNumberArgument % 73 + favEpisodeNumberOfTheOffice * Math.PI;
  return 'This cohort is the best cohort!!!';
}

export default whatCohortIsBest;
