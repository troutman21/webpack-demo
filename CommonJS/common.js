// Step 3 require greetings from greetings.js
const myGreetings = require('./greetings.js');

// Step 4: run greeting method show we have access to the variable
myGreetings.sayHi();

//Bonus: what happens when we add a new name variable here?
const name = 'Will';